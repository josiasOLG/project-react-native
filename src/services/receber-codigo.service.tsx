/* eslint-disable prettier/prettier */
import { API_BASE_URL } from '../config/constants/api';
import BaseService from './base.service';

class ReceberCodigoService extends BaseService {
  constructor() {
    super('');
  }
}

export default new ReceberCodigoService();
